import styled from 'styled-components'

export const NavbarContainer = styled.div`
   display: flex;
   justify-content: right;
   align-items: center;
`

export const SignInSignOutText = styled.p`
   text-align: right;
   font-size: 20px;
   text-decoration: underline;
   cursor: pointer;
   margin-right: 10px;
`